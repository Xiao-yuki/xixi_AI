
last_keyword = "none"#初始化

class keyword_search_system:
    def keyword_search(string):
        keyword = "none"#暫時 之後補上提取模型
        if last_keyword == keyword:
            keyword = "none"
        else:
            last_keyword = keyword
        return keyword